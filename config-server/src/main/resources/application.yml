server:
    port: 9296

spring:
    application:
        name: CONFIG-SERVER
    cloud:
        config:
            server:
                git:
                    uri: https://github.com/mnibras/microservices-config-repo
                    default-label: master
                    clone-on-start: true
